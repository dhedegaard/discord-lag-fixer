trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: SSH@0
  inputs:
    sshEndpoint: 'dennis@ymir'
    runOptions: 'commands'
    commands: 'bash -e -x -c "\
        cd discord-lag-fixer && \
        git pull --ff-only && \
        docker-compose pull && \
        docker-compose up --build -d
      "'
